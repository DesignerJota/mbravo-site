# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# PAYMENT AND NOTIFICATION GATEWAY INTEGRATION
PAYMENT_MODE="test" # Set to "live" for production transactions, "test" for sandbox simulation
STRIPE_SECRET_KEY="" # For real credit card payment intents (Secret Key - server side only)
STRIPE_PAYMENT_METHOD_CONFIGURATION_ID="" # Stripe Custom Payment Method configuration ID (e.g. pmc_1T...)
VITE_STRIPE_PUBLISHABLE_KEY="" # For client-side checkout and elements (Publishable Key)
IFTHENPAY_MB_KEY="" # For Portuguese Multibanco reference generation (optional)
IFTHENPAY_MBWAY_KEY="" # For Portuguese MB WAY authorization (optional)

# EMAIL TRANSACTIONAL SYSTEM
SENDGRID_API_KEY="" # Add SendGrid token to active automatic mail dispatches
FROM_EMAIL="encomendas@mbravobycarolina.com" # Sender address for purchase receipts
ADMIN_NOTIFY_EMAIL="handmade@mbravobycarolina.com" # Notification destination for atelier management (Carolina)

# ADMIN DASHBOARD ACCESS
ADMIN_PASSWORD="CarolinaM26"

# DYNAMIC SOCIAL & FEED INTEGRATION
VITE_BEHOLD_WIDGET_ID="" # Your Behold.so Instagram widget ID (e.g. 1a2b3c4d5e)
GOOGLE_PLACES_API_KEY="" # Your Google Places API Key to pull live reviews natively (Server side only)
GOOGLE_PLACE_ID="" # Your Google Place ID for M★BRAVO (e.g. ChIJSzO...)

# DATABASE INTEGRATION
DATABASE_URL="postgresql://postgres:DesignerJota83$$@db.trmsteycllxspudgpxsu.supabase.co:5432/postgres" # Connection string for Supabase PostgreSQL


